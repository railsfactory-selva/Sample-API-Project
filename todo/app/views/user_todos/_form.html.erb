<%= form_for(@user_todo) do |f| %>
  <% if @user_todo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_todo.errors.count, "error") %> prohibited this user_todo from being saved:</h2>

      <ul>
      <% @user_todo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :completed %><br>
    <%= f.check_box :completed %>
  </div>
  <div class="field">
    <%= f.label :order %><br>
    <%= f.number_field :order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
